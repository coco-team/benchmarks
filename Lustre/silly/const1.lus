const c = 0.2;

-- this example has no meaning. It is used to illustrate const propagation and simplification of expressions
node toto (x: real) returns (y, z: real);
var u1, u2, u3, u4: real;
let
  y = c -> (0.2 * pre y) + x;
  z = x + c;
  u1 = pre u2;
  u2 = u3 - u4;
  u3 = x;
  u4 = c + x;
tel